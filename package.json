{
  "name": "@robinheat/i18n-mcp",
  "version": "0.1.0",
  "description": "MCP server for managing i18n JSON translation files",
  "type": "module",
  "files": [
    "dist/",
    "skills/"
  ],
  "bin": {
    "i18n-mcp": "dist/server.js"
  },
  "scripts": {
    "build": "tsc && chmod +x dist/server.js",
    "dev": "tsx src/server.ts",
    "test": "vitest run",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "minimatch": "^10.0.1",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.0",
    "vitest": "^3.1.0"
  }
}
